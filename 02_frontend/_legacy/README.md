# Legacy Frontend Items

This folder contains frontend files quarantined during `chore/repo-structure-v1`.

## Why this exists
- Items were verified as unused by current app entry (`src/main.tsx` -> `App.tsx`).
- Preserved for rollback safety and historical reference.

## Contents
- `ResultsDashboard.dashboard.tsx`: superseded dashboard variant not imported by current UI flow.

## Removal policy
- Delete permanently after one stable release cycle if still unreferenced.
